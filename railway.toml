[build]
builder = "NIXPACKS"
buildCommand = "cd cloudshareapi/cloudshareapi && mvn clean install -DskipTests"

[deploy]
startCommand = "cd cloudshareapi/cloudshareapi && java -jar target/cloudshareapi-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
