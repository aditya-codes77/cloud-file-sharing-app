[build]
builder = "NIXPACKS"
watchPatterns = ["cloudshareapi/**"]

[deploy]
startCommand = "java -jar target/cloudshareapi-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
